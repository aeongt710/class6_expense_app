import React from 'react';



export const Trans=({transaction})=>{
        return (
        <div>
            jasd
            <li className={"plus"}>
                {Trans.desc}
                <span style={{textAlign: "left"}}>${Trans.amount}</span>
            </li>
        </div>
    )
};